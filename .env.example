# Required: Vercel AI Gateway API key (never commit a real key)
AI_GATEWAY_API_KEY=

# Optional
# HEADLESS=false   # visible window (default is headless). Use this if Cloudflare shows "Just a moment".
# MAX_STEPS=50     # stop after N decisions (default: 50)
