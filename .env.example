# Required: Vercel AI Gateway API key (never commit a real key)
# https://vercel.com/docs/ai-gateway
AI_GATEWAY_API_KEY=

# Optional
# Headless Chromium is blocked by Cookie Clicker's Cloudflare check.
# Unset: visible window when DISPLAY is set, otherwise headless.
# HEADLESS=false
# MAX_STEPS=50
